<?xml version="1.0" encoding="utf-8"?>
<!--suppress ALL -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sadi.sreda">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Permission to start Alarm on device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen"
            android:screenOrientation="portrait"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"
            android:screenOrientation="portrait"
            />

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"
            />

        <activity android:name=".SettingsActivity"
            android:screenOrientation="portrait"
            />

        <activity android:name=".alarm.AlarmMainActivity"
            android:screenOrientation="portrait"
            />

        <activity android:name=".ProfileSettingsActivity"
            android:screenOrientation="portrait"
            />
        <activity android:name=".MyRecordsActivity"
            android:screenOrientation="portrait"
            />

        <!--  Declare  Alarm Broadcast receiver here  -->
        <receiver android:name=".alarm.AlarmReceiver" />

        <!--  Declare  Alarm Notification Service here  -->
        <service android:name=".alarm.AlarmNotificationService" />

        <!--  Declare  Alarm Sound Service here  -->
        <service android:name=".alarm.AlarmSoundService" />


        <!-- The receiver for the buyer push notifications that are *not* cleared -->
        <receiver android:name=".alarm.BuyerPushNotif$FirstPushNotif" >
            <intent-filter>
                <action android:name="first_notif_action" />
            </intent-filter>
        </receiver>

        <!-- The receiver for the buyer push notifications that are *not* cleared -->
        <receiver android:name=".alarm.BuyerPushNotif$SecondPushNotif" >
            <intent-filter>
                <action android:name="second_notif_action" />
            </intent-filter>
        </receiver>

        <!-- The receiver for the third push notifications that are *not* cleared -->
        <receiver android:name=".alarm.BuyerPushNotif$ThirdPushNotif" >
            <intent-filter>
                <action android:name="third_notif_action" />
            </intent-filter>
        </receiver>

        <!-- The receiver for the push notifications that *are* cleared -->
        <receiver android:name=".alarm.BuyerPushNotif$ClearedPushNotif" >
            <intent-filter>
                <action android:name="notification_cancelled" />
            </intent-filter>
        </receiver>
        <receiver android:name=".alarm.BuyerPushNotif" />
    </application>

</manifest>